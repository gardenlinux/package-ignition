on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'
jobs:
  build:
    uses: gardenlinux/package-build/.github/workflows/build.yml@main
    with:
      release: ${{ github.ref == 'refs/heads/main' }}
      build_dep: |
        gardenlinux/package-golang-github-containers-libhvee 0.7.1+ds1-1gardenlinux0
        gardenlinux/bp-package-golang-github-azure-sdk-for-go 68.0.0+git20231220.f497335+dfsg1-5gl1+bp2150
        gardenlinux/package-golang 1.26.5-1gl0+bp2150
        gardenlinux/bp-package-golang-defaults 1.26gl0+bp2150
